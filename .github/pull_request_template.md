## O que mudou

-

## Validacao

- [ ] `python -m pytest`
- [ ] `python -m compileall app examples tests`

## Deploy

- O deploy na VPS acontece automaticamente depois do merge em `main`, pelo workflow `Deploy VPS`.
- O workflow preserva `.env`, `.env.txt` e bancos locais na VPS.

